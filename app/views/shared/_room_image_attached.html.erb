<% width = local_assigns[:size] || 40 %>
<% width = local_assigns[:size] || 40 %>

<% if obj.image.attached? %>
  <%= image_tag obj.image.variant(resize_to_limit: [width, width]), class:"" %>
<% else %>
  <%= image_tag "default-image-hotel.png", class: "", size: "#{width}x#{width}" %>
<% end %>