<!DOCTYPE html>
<html>
  <head>
    <title>Mytaskapp2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

     <!--Bootstrap 5 CSSをCDNで読み込む -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <header>
    <h5>宿泊予約アプリ</h5>
    <div class="header_contents">
      <div><%= link_to "Mountbook Share", root_path, class: "toppage_link"%></div>
        <div class="login_and_signup_buttons">
          <% if user_signed_in? %>
            <!-- 以下でナビゲーションバー(bootstrap)を表示-->
            <%= render "shared/navigation_bar"%>
          <% else %>
            <div><%= link_to "ログイン", new_user_session_path, class: "btn btn-light", style:"color: black;" %></div>
            <div><%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary", style:"color: white;" %></div>
          <% end %>
        </div>
      </div>
    </div>
  </header>

  <body>
    <div class="container-fluid">
      <%= yield %>
      <!-- ↓ここに書くとどのページでもフラッシュが表示される。-->
      <%= render "devise/shared/flash_message" %>
    </div>
  </body>
</html>


